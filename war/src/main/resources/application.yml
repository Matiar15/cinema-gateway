spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cinema_gateway?allowPublicKeyRetrieval=true&useSSL=false
    username: test
    password: test
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        ddl-auto: update

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type: TRACE

server:
  servlet:
    context-path: /cinema-gateway